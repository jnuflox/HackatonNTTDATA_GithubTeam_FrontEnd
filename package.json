{
  "name": "app-ia-projects",
  "version": "1.0.0",
  "description": "Frontend para gestión de proyectos con IA",
  "scripts": {
    "start": "http-server -p 8080 -c-1 --cors",
    "dev": "http-server -p 8080 -c-1 --cors -o"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
